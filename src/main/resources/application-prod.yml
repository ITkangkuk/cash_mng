server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cash_mng
    username: cash_user
    password: 운영DB비밀번호
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  data:
    redis:
      host: localhost
      port: 6379

logging:
  file:
    name: /home/ec2-user/app/cash_mng/logs/app.log
  level:
    root: info
    org.springframework: info